---
name: xxx
description: [做什么 + 何时调用，写得"pushy"一点覆盖用户的各种措辞。**前 249 字符是实际可见预算**，承重内容必须前置，详见下方说明]
---

# 一句话定场（What this skill does, 1-2句）

## Working Protocol
[具体步骤，每步顺带说明"为什么"，而不是单列 Reasoning Framework]
[>500行就拆到 references/xxx.md，正文只做路由]

## Exit Criteria
[具体、可判定的完成条件，不是抽象描述]

## Exit Protocol
[收尾动作：产出什么、放哪里、如何呈现给用户]

---

## description 的硬约束（实测，非推测）

模型决定要不要加载这个 skill 时，看到的 **只有 `description`**，而且它**被截断到前 249 字符**（来源：ZCode 运行时 `formatSkillLine`，常量 `250`）。

两条结论：

1. **承重内容必须前置**：做什么 + 何时调用 + 最容易误触发的边界，都要落在前 249 字符内。写在这之后的等于没写。
2. **上限 1024 字符**（Agent Skills 标准，跨客户端一致），超了 skill 会被**直接丢弃**。249 是显示截断、1024 是生死线，两者不同。

`description` 只写"这个 skill 干什么、什么时候用"就够了——**不要把行为规范、反模式、详细边界塞进去**，那些属于正文的「守卫」章节。写得过长是把触发信息推到看不见的地方，适得其反。

写完自查：

```bash
python3 - <<'PY'
import re
fm = re.match(r'^---\n(.*?)\n---\n', open('SKILL.md', encoding='utf-8').read(), re.S).group(1)
d = re.search(r'^description:\s*(.+)$', fm, re.M).group(1).strip()
print(f"description {len(d)}/249 →", "✓" if len(d) <= 249 else f"✗ 尾部 {len(d)-249} 字符不可见")
PY
```

### 关于 `when_to_use`

Agent Skills 标准里**没有**这个字段；只有 Claude Code 和 ZCode 认它，Codex 和 pi 静默忽略。**本仓库不使用它。**

不用它的理由：同一个 skill 在两个客户端会读到不同的触发信息，行为分叉，难测也难推理；而且它被拼在 `description` **之后**一起算那 249 字符，描述稍长它就完全不可见，等于白写。边界信息交给正文的「守卫」章节即可。